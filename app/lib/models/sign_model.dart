class SignModel {
  String id;
  String clazzId;
  String startTime;
  String type;
  String className;
  int status;

  SignModel(this.id, this.clazzId, this.startTime, this.type, this.className,
      this.status);
}
